


# tryCatch(
#   {
#     `%dopar` <- foreach::`%dopar%`
#   },
#   error = function(e) {
#     message("fmconfer uses functions from the foreach package. Please install
#             foreach to access the parallel processing features.")
#     print(e)
#   }
# )

# library(devtools)
